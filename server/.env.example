NODE_ENV=production
PORT=5000

# JWT
JWT_SECRET=change-me

# CORS
CLIENT_ORIGIN=https://your-vercel-app.vercel.app

# Database via Render (preferred)
DATABASE_URL=postgres://user:password@host:5432/dbname
DB_SSL=true

# Or discrete settings (when not using DATABASE_URL)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_DIALECT=postgres
